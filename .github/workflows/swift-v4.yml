name: Swift

on:
  push:
    branches: [ master, v4 ]
  pull_request:
    branches: [ master, v4 ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
